group = 'com.clover'

apply from: file("${androidBuild}/app.gradle")

android.defaultConfig {
    minSdkVersion 15
    targetSdkVersion 26
}


android {
    compileSdkVersion 26
}

dependencies {
    implementation 'com.clover.sdk:clover-android-sdk:222'
}
